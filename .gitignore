node_modules/
dist/
coverage/
*.log
.DS_Store
.env
.env.local
.vscode/
.idea/
__pycache__/

# LongMemEval harness — per-run result artifacts are not committed; baselines
# are summarized in Ventures/nlm-memory/track-record.md instead.
reports/longmemeval/

# Local development logs (session CHANGELOG, daily-digest cron output, etc.) —
# kept on disk for personal history but never tracked in git.
logs/
.worktrees/

# Personal sub-project guide for the orchestrator — contains absolute paths to
# Edward's local vault + Whtnxt Agent workspace, not useful to other contributors.
CLAUDE.md
